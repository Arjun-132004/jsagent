apiVersion: v1
kind: Service
metadata:
  name: jsagent-app
  namespace: storefront-devpython
  labels:
    app.kubernetes.io/name: jsagent-app
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: jsagent-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
