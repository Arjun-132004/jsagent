apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: jsagent-app-stripe-secret-key
  namespace: storefront-devpython
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: STRIPE_SECRET_KEY
      remoteRef:
        key: teams-dev/storefront-devpython/STRIPE_SECRET_KEY
