apiVersion: v1
kind: Service
metadata:
  name: jsagent-client
  namespace: storefront-devjs
  labels:
    app.kubernetes.io/name: jsagent-client
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: jsagent-client
  ports:
    - name: http
      port: 80
      targetPort: 8080
